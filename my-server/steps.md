on your terminal: 
1: 
install the AI model (ollama) : 
curl -fsSL https://ollama.com/install.sh | sh

2: run it 

ollama run mistral

3: install axios: npm install axios

check if this works we can test it with data later
